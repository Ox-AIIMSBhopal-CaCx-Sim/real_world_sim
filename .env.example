# API (local emulator)
GCS_EMULATOR_HOST=http://localhost:4443
GCS_BUCKET=sim-results
GCS_PROJECT=local-dev
SIM_OUTPUT_ROOT=./backend/sim_results

# Frontend (Vite) — leave empty in Docker/Cloud Run for same-origin /api
VITE_API_BASE=http://localhost:8000

# Production (Cloud Run) — unset GCS_EMULATOR_HOST
# GCS_BUCKET=pathology-simulation
# GCS_PROJECT=marine-shell-490317-u9
